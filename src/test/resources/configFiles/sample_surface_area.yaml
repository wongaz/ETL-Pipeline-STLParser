pipelineConfigs:
  # Simple
  - pipelineName: simple
    extractType: file
    extractConfiguration:
      file: STLFiles/simple.stl
    parseFormat: stl
    statistics:
      - area
    statisticsConf:
      metric: euclidean
    loadType:
      - stdout
    loadConf:
