pipelineConfigs:
  # Simple
  - pipelineName: simple
    extractType: file
    extractConfiguration:
      file: STLFiles/simple.stl
    parseFormat: stl
    statistics:
      - box
    statisticsConf:
      metric: euclidean
    loadType:
      - stdout